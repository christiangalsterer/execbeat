execbeat:

   commands:
    -
      schedule: "@every 1m"
      command: echo1
      args: "Execbeat"
      fields:
        foo1: bar1
        foo2: bar2
      document_type: exec
    -
      schedule: "@every 2m"
      command: echo2
      args: "Hello World"

